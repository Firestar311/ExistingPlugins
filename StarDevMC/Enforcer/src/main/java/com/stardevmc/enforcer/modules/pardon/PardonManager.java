package com.stardevmc.enforcer.modules.pardon;

import com.stardevmc.enforcer.Enforcer;
import com.stardevmc.enforcer.modules.base.Manager;

public class PardonManager extends Manager {
    public PardonManager(Enforcer plugin) {
        super(plugin, "pardon", false);
    }
    
    public void saveData() {
    
    }
    
    public void loadData() {
    
    }
}